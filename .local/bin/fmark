#!/bin/sh
file=$HOME/.config/fzf-marks/.fzf-fmarks
temp=/tmp/.fzf-fmarks.tmp

realpath "$1" && realpath "$1" >> "$file";
sed -i 's|/home/thedro|~|g' "$file";

awk '!visited[$0]++' "$file" > "$temp";
mv -f "$temp" "$file";
rm -f "$temp";
